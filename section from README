[1mdiff --git a/.claude/settings.local.json b/.claude/settings.local.json[m
[1mnew file mode 100644[m
[1mindex 0000000..0f59bfc[m
[1m--- /dev/null[m
[1m+++ b/.claude/settings.local.json[m
[36m@@ -0,0 +1,7 @@[m
[32m+[m[32m{[m
[32m+[m[32m  "permissions": {[m
[32m+[m[32m    "allow": [[m
[32m+[m[32m      "Bash(git -C tuanaelifproje diff experiment.py)"[m
[32m+[m[32m    ][m
[32m+[m[32m  }[m
[32m+[m[32m}[m
[1mdiff --git a/experiment.py b/experiment.py[m
[1mindex fb5c732..d7dae68 100644[m
[1m--- a/experiment.py[m
[1m+++ b/experiment.py[m
[36m@@ -46,7 +46,7 @@[m [mdef _probit(p: float) -> float:[m
     c = [2.515517, 0.802853, 0.010328][m
     d = [1.432788, 0.189269, 0.001308][m
     x = t - (c[0] + c[1]*t + c[2]*t*t) / (1 + d[0]*t + d[1]*t*t + d[2]*t*t*t)[m
[31m-    return x if p < 0.5 else -x[m
[32m+[m[32m    return -x if p < 0.5 else x[m
 [m
 [m
 class Experiment:[m
[36m@@ -161,16 +161,6 @@[m [mclass Experiment:[m
         CR rate     = DS− basılmayış / toplam DS− trial[m
         d'          = Z(hit) − Z(false_alarm)[m
         """[m
[31m-        ds_plus_trials  = self.stats["rewarded"] + self.stats["punished"] + self.stats["omission"][m
[31m-        ds_minus_trials = self.stats["correct_rejection"] + self.stats["punished"] + self.stats["rewarded"][m
[31m-[m
[31m-        # DS+ basışları (outcome ne olursa olsun basılmış DS+ triallar)[m
[31m-        ds_plus_hits = sum(1 for _ in range(self.stats["rewarded"] + self.stats["punished"])[m
[31m-                           if True)  # placeholder; gerçek sayım aşağıda[m
[31m-[m
[31m-        # Daha temiz hesap: direkt counter'lardan[m
[31m-        # Hit = lever basılan DS+ triallar[m
[31m-        # False alarm = lever basılan DS- triallar[m
         total_ds_plus  = sum(1 for ds in self.trial_sequence[:self.trial_num] if ds == DSType.PLUS)[m
         total_ds_minus = sum(1 for ds in self.trial_sequence[:self.trial_num] if ds == DSType.MINUS)[m
 [m
[36m@@ -262,14 +252,6 @@[m [mclass Experiment:[m
         self._ds_event.set()[m
         self._dout_event.set()[m
         self._in_iti = False[m
[31m-        self.box.lever_retract(config.LEVER_SIDE)[m
[31m-        self.box.house_light_off()[m
[31m-        self.box.cue_light_off(config.LEVER_SIDE)[m
[31m-        self.box.shock(False)[m
[31m-        self.state = State.IDLE[m
[31m-        self._emit_state()[m
[31m-        if self._csv_file:[m
[31m-            self._csv_file.close()[m
         self.log.info("Deney durduruldu")[m
 [m
     # ── Ana döngü ─────────────────────────────────────────────────────────────[m
[36m@@ -292,10 +274,13 @@[m [mclass Experiment:[m
                 )[m
         finally:[m
             self._in_iti = False[m
[32m+[m[32m            self.box.shock(False)[m
[32m+[m[32m            self.box.cue_light_off(config.LEVER_SIDE)[m
             self.box.house_light_off()[m
             self.box.lever_retract(config.LEVER_SIDE)[m
             if self._csv_file:[m
                 self._csv_file.close()[m
[32m+[m[32m                self._csv_file = None[m
             if self.state != State.IDLE:[m
                 self.state = State.IDLE[m
                 self._emit_state()[m
[36m@@ -414,6 +399,8 @@[m [mclass Experiment:[m
                     self._stop_event.wait(config.WATER_PULSE_GAP_S)[m
                 self._stop_event.wait(config.LICK_WINDOW_S)[m
                 self._counting_licks = False[m
[32m+[m[32m                r, g, b = config.HOUSE_LIGHT_COLOR[m
[32m+[m[32m                self.box.house_light(r, g, b)[m
                 self.log.info(f"Trial {self.trial_num} — Lick: {self.lick_count}")[m
             else:[m
                 result = TrialResult.PUNISHED[m
[36m@@ -429,6 +416,8 @@[m [mclass Experiment:[m
                 self.box.shock(True)[m
                 self._stop_event.wait(config[1mdiff --git a/.claude/settings.local.json b/.claude/settings.local.json[m
[1mnew file mode 100644[m
[1mindex 0000000..0f59bfc[m
[1m--- /dev/null[m
[1m+++ b/.claude/settings.local.json[m
[36m@@ -0,0 +1,7 @@[m
[32m+[m[32m{[m
[32m+[m[32m  "permissions": {[m
[32m+[m[32m    "allow": [[m
[32m+[m[32m      "Bash(git -C tuanaelifproje diff experiment.py)"[m
[32m+[m[32m    ][m
[32m+[m[32m  }[m
[32m+[m[32m}[m
[1mdiff --git a/experiment.py b/experiment.py[m
[1mindex fb5c732..d7dae68 100644[m
[1m--- a/experiment.py[m
[1m+++ b/experiment.py[m
[36m@@ -46,7 +46,7 @@[m [mdef _probit(p: float) -> float:[m
     c = [2.515517, 0.802853, 0.010328][m
     d = [1.432788, 0.189269, 0.001308][m
     x = t - (c[0] + c[1]*t + c[2]*t*t) / (1 + d[0]*t + d[1]*t*t + d[2]*t*t*t)[m
[31m-    return x if p < 0.5 else -x[m
[32m+[m[32m    return -x if p < 0.5 else x[m
 [m
 [m
 class Experiment:[m
[36m@@ -161,16 +161,6 @@[m [mclass Experiment:[m
         CR rate     = DS− basılmayış / toplam DS− trial[m
         d'          = Z(hit) − Z(false_alarm)[m
         """[m
[31m-        ds_plus_trials  = self.stats["rewarded"] + self.stats["punished"] + self.stats["omission"][m
[31m-        ds_minus_trials = self.stats["correct_rejection"] + self.stats["punished"] + self.stats["rewarded"][m
[31m-[m
[31m-        # DS+ basışları (outcome ne olursa olsun basılmış DS+ triallar)[m
[31m-        ds_plus_hits = sum(1 for _ in range(self.stats["rewarded"] + self.stats["punished"])[m
[31m-                           if True)  # placeholder; gerçek sayım aşağıda[m
[31m-[m
[31m-        # Daha temiz hesap: direkt counter'lardan[m
[31m-        # Hit = lever basılan DS+ triallar[m
[31m-        # False alarm = lever basılan DS- triallar[m
         total_ds_plus  = sum(1 for ds in self.trial_sequence[:self.trial_num] if ds == DSType.PLUS)[m
         total_ds_minus = sum(1 for ds in self.trial_sequence[:self.trial_num] if ds == DSType.MINUS)[m
 [m
[36m@@ -262,14 +252,6 @@[m [mclass Experiment:[m
         self._ds_event.set()[m
         self._dout_event.set()[m
         self._in_iti = False[m
[31m-        self.box.lever_retract(config.LEVER_SIDE)[m
[31m-        self.box.house_light_off()[m
[31m-        self.box.cue_light_off(config.LEVER_SIDE)[m
[31m-        self.box.shock(False)[m
[31m-        self.state = State.IDLE[m
[31m-        self._emit_state()[m
[31m-        if self._csv_file:[m
[31m-            self._csv_file.close()[m
         self.log.info("Deney durduruldu")[m
 [m
     # ── Ana döngü ─────────────────────────────────────────────────────────────[m
[36m@@ -292,10 +274,13 @@[m [mclass Experiment:[m
                 )[m
         finally:[m
             self._in_iti = False[m
[32m+[m[32m            self.box.shock(False)[m
[32m+[m[32m            self.box.cue_light_off(config.LEVER_SIDE)[m
             self.box.house_light_off()[m
             self.box.lever_retract(config.LEVER_SIDE)[m
             if self._csv_file:[m
                 self._csv_file.close()[m
[32m+[m[32m                self._csv_file = None[m
             if self.state != State.IDLE:[m
                 self.state = State.IDLE[m
                 self._emit_state()[m
[36m@@ -414,6 +399,8 @@[m [mclass Experiment:[m
                     self._stop_event.wait(config.WATER_PULSE_GAP_S)[m
                 self._stop_event.wait(config.LICK_WINDOW_S)[m
                 self._counting_licks = False[m
[32m+[m[32m                r, g, b = config.HOUSE_LIGHT_COLOR[m
[32m+[m[32m                self.box.house_light(r, g, b)[m
                 self.log.info(f"Trial {self.trial_num} — Lick: {self.lick_count}")[m
             else:[m
                 result = TrialResult.PUNISHED[m
[36m@@ -429,6 +416,8 @@[m [mclass Experiment:[m
                 self.box.shock(True)[m
                 self._stop_event.wait(config.SHOCK_DURATION_S)[m
                 self.box.shock(False)[m
[32m+[m[32m                r, g, b = config.HOUSE_LIGHT_COLOR[m
[32m+[m[32m                self.box.house_light(r, g, b)[m
         else:[m
             if ds_type == DSType.PLUS:[m
                 result = TrialResult.OMISSION[m
[1mdiff --git a/main.py b/main.py[m
[1mindex 37a0f4e..cf25051 100644[m
[1m--- a/main.py[m
[1m+++ b/main.py[m
[36m@@ -8,6 +8,8 @@[m [mimport tkinter as tk[m
 from tkinter import ttk, messagebox, scrolledtext, filedialog[m
 import logging[m
 import os[m
[32m+[m[32mimport threading[m
[32m+[m[32mimport time[m
 import webbrowser[m
 from datetime import datetime[m
 [m
[36m@@ -634,14 +636,16 @@[m [mclass App(tk.Tk):[m
         logging.getLogger("HW").info("Lever geri alındı")[m
 [m
     def _hw_water(self):[m
[31m-        import threading[m
         pulses = int(self.var_water_pulses.get())[m
[31m-        def _run():[m
[31m-            for _ in range(pulses):[m
[31m-                self.box.water(config.WATER_SIDE)[m
[31m-                import time; time.sleep(config.WATER_PULSE_GAP_S)[m
[31m-        threading.Thread(target=_run, daemon=True).start()[m
[31m-        logging.getLogger("HW").info(f"Su: {pulses} pulse")[m
[32m+[m[32m        gap_ms = int(config.WATER_PULSE_GAP_S * 1000)[m
[32m+[m[32m        log = logging.getLogger("HW")[m
[32m+[m[32m        log.info(f"Su: {pulses} pulse")[m
[32m+[m[32m        def _send_pulse(remaining):[m
[32m+[m[32m            if remaining <= 0:[m
[32m+[m[32m                return[m
[32m+[m[32m            self.box.water(config.WATER_SIDE)[m
[32m+[m[32m            self.after(gap_ms, lambda: _send_pulse(remaining - 1))[m
[32m+[m[32m        _send_pulse(pulses)[m
 [m
     def _hw_shock(self):[m
         dur = self._hw_shock_dur.get()[m
[1mdiff --git a/operant_box.py b/operant_box.py[m
[1mindex 7cdbb6a..9c769fc 100644[m
[1m--- a/operant_box.py[m
[1m+++ b/operant_box.py[m
[36m@@ -62,7 +62,8 @@[m [mclass OperantBox:[m
         try:[m
             self._serial = serial.Serial([m
                 self.port, self.BAUD_RATE,[m
[31m-                bytesize=8, parity='N', stopbits=1, timeout=0.1[m
[32m+[m[32m                bytesize=8, parity='N', stopbits=1, timeout=0.1,[m
[32m+[m[32m                write_timeout=1.0[m
             )[m
             self._running = True[m
             self._thread = threading.Thread(target=self._read_loop, daemon=True)[m
[36m@@ -187,7 +188,12 @@[m [mclass OperantBox:[m
         if self.simulated:[m
             return[m
         if self._serial and self._serial.is_open:[m
[31m-            self._serial.write(data)[m
[32m+[m[32m            try:[m
[32m+[m[32m                self._serial.write(data)[m
[32m+[m[32m            except serial.SerialTimeoutException:[m
[32m+[m[32m                self.log.error("Serial yazma timeout — donanım yanıt vermiyor")[m
[32m+[m[32m            except serial.SerialException as e:[m
[32m+[m[32m                self.log.error(f"Serial yazma hatası: {e}")[m
         else:[m
             self.log.warning("Seri port kapalı, paket gönderilemedi")[m
 [m
[1mdiff --git a/tuanaelifproje b/tuanaelifproje[m
[1mnew file mode 160000[m
[1mindex 0000000..b164ae1[m
[1m--- /dev/null[m
[1m+++ b/tuanaelifproje[m
[36m@@ -0,0 +1 @@[m
[32m+[m[32mSubproject commit b164ae1b96771ee31146e156d317c17d11329969[m
